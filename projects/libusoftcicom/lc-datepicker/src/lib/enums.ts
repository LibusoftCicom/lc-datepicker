export enum ECalendarType {
    Time,
    DateTime,
    Date,
    MonthYear,
    Year,
    DateRange
}

export enum ECalendarNavigation {
    Up,
    Right,
    Down,
    Left,
    PageUp,
    PageDown,
    Confirm,
    Close
}

export enum EHourFormat {
  TWENTY_FOUR_HOUR,
  TWELVE_HOUR
}

export enum DateType {
  REGULAR,
  FROM,
  TO,
}
